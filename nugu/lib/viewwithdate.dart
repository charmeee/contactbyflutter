import 'package:flutter/material.dart';
class  viewwithdate extends StatelessWidget {
  const viewwithdate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("calender"),
    );
  }
}